#!/usr/bin/perl -w

=head1

stuffdb - populates an empty db with schemas, tables, and data

=cut

use strict;
use warnings;

use StuffDB;

StuffDB->run;

=head1 SYNOPSIS

stuffdb [options]

Options:

	--config		Path to config file if not in current directory (optional)
	--schemas 	Schema to create (optional, repeatable)
	--commands	Commands to run to help setup db, scripts will be run in order
								given (optional, repeatable)
	--help			Show this message

=cut
